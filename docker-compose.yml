services:

  # ── MediaFlow Proxy ───────────────────────────────────────────────────────────
  # Proxies and remuxes RD streams to HLS so the browser <video> tag can play
  # any format (MKV, AVI, etc.) natively. Exposed on port 8888.
  # Set MEDIAFLOW_PUBLIC_URL=http://<your-server-ip>:8888 in your stack env vars.
  mediaflow:
    image: mhdzumair/mediaflow-proxy
    container_name: mediaflow
    restart: unless-stopped
    ports:
      - "8888:8888"
    environment:
      - API_PASSWORD=${MEDIAFLOW_API_PASSWORD:-heartstream}
      - TRANSCODE_VIDEO_PRESET=${TRANSCODE_VIDEO_PRESET:-ultrafast}
    networks:
      - heartstream-network

  # ── Jackettio ─────────────────────────────────────────────────────────────────
  # Bridges torrent indexers + Real-Debrid into a single stream API.
  jackettio:
    build: https://github.com/arvida42/jackettio.git
    container_name: jackettio
    restart: unless-stopped
    depends_on:
      - mediaflow
    environment:
      - PORT=4000
      - TMDB_ACCESS_TOKEN=${TMDB_ACCESS_TOKEN}
      - DATA_FOLDER=/data
    volumes:
      - jackettio-data:/data
    networks:
      - heartstream-network

  # ── HeartStream ───────────────────────────────────────────────────────────────
  heartstream:
    build:
      context: .
      dockerfile: server/Dockerfile
    container_name: heartstream-v2
    restart: unless-stopped
    depends_on:
      - jackettio
      - mediaflow
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - TMDB_API_KEY=${TMDB_API_KEY}
      - TMDB_ACCESS_TOKEN=${TMDB_ACCESS_TOKEN}
      - JACKETTIO_URL=http://jackettio:4000
      - REALDEBRID_API_KEY=${REALDEBRID_API_KEY}
      - JACKETTIO_INDEXERS=${JACKETTIO_INDEXERS:-thepiratebay,yts,eztv,therarbg}
      - JACKETTIO_QUALITIES=${JACKETTIO_QUALITIES:-720,1080,2160}
      - JACKETTIO_BLACKLIST=${JACKETTIO_BLACKLIST:-}
      - MEDIAFLOW_PUBLIC_URL=${MEDIAFLOW_PUBLIC_URL:-}
      - MEDIAFLOW_INTERNAL_URL=${MEDIAFLOW_INTERNAL_URL:-http://mediaflow:8888}
      - MEDIAFLOW_API_PASSWORD=${MEDIAFLOW_API_PASSWORD:-heartstream}
    volumes:
      - heartstream-data:/data
    networks:
      - heartstream-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

volumes:
  jackettio-data:
  heartstream-data:

networks:
  heartstream-network:
    driver: bridge
